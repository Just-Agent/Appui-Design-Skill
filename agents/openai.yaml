interface:
  display_name: "App UI Design Skill"
  short_description: "Design app UI screens and theme suites with source-linked previews."
  default_prompt: "Use $appui-design-skill to design a multi-screen mobile app UI theme with source-linked screenshots."
policy:
  allow_implicit_invocation: true
